///scr_push
//HORIZONTAL
/*if (place_meeting(x+xxmovestep, y, obj_crate03)) {

    var block = instance_place(x+xxmovestep, y, obj_crate03);
    with(block){
    x+=(other.xxmovestep*1.1)
    //scr_movenet(zombie_dx/2,0);
    
    }
xxmovestep /= 2;
}

//VERTICAL
if (place_meeting(x,y+yymovestep, obj_crate03)) {

    var block = instance_place(x,y+yymovestep,  obj_crate03);
    with(block){
    y+=(other.yymovestep*1.1)
    //scr_movenet(zombie_dy/2,0);
    
    }
yymovestep /= 2;
}



//2eme objet

if (place_meeting(x+xxmovestep, y, obj_shape05)) {

    var block2 = instance_place(x+xxmovestep, y, obj_shape05);
    with(block2){
    x+=(other.xxmovestep*1.1)
    //scr_movenet(zombie_dx/2,0);
    
    }
xxmovestep /= 2;
}

//VERTICAL
if (place_meeting(x,y+yymovestep, obj_shape05)) {

    var block2 = instance_place(x,y+yymovestep,  obj_shape05);
    with(block2){
    y+=(other.yymovestep*1.1)
    //scr_movenet(zombie_dy/2,0);
    
    }
yymovestep /= 2;
}


//3eme objet

if (place_meeting(x+xxmovestep, y, obj_crate02)) {

    var block3 = instance_place(x+xxmovestep, y, obj_crate02);
    with(block3){
    x+=(other.xxmovestep*1.1)
    //scr_movenet(zombie_dx/2,0);
    
    }
xxmovestep /= 2;
}

//VERTICAL
if (place_meeting(x,y+yymovestep, obj_crate02)) {

    var block3 = instance_place(x,y+yymovestep,  obj_crate02);
    with(block3){
    y+=(other.yymovestep*1.1)
    //scr_movenet(zombie_dy/2,0);
    
    }
yymovestep /= 2;
}



//4eme objet

if (place_meeting(x+xxmovestep, y, obj_shape03)) {

    var block4 = instance_place(x+xxmovestep, y, obj_shape03);
    with(block4){
    x+=(other.xxmovestep*1.1)
    //scr_movenet(zombie_dx/2,0);
    
    }
xxmovestep /= 2;
}

//VERTICAL
if (place_meeting(x,y+yymovestep, obj_shape03)) {

    var block4 = instance_place(x,y+yymovestep,  obj_shape03);
    with(block4){
    y+=(other.yymovestep*1.1)
    //scr_movenet(zombie_dy/2,0);
    
    }
yymovestep /= 2;
}


//5eme objet

if (place_meeting(x+xxmovestep, y, obj_shape06)) {

    var block5 = instance_place(x+xxmovestep, y, obj_shape06);
    with(block5){
    x+=(other.xxmovestep*1.1)
    //scr_movenet(zombie_dx/2,0);
    
    }
xxmovestep /= 2;
}

//VERTICAL
if (place_meeting(x,y+yymovestep, obj_shape06)) {

    var block5 = instance_place(x,y+yymovestep,  obj_shape06);
    with(block5){
    y+=(other.yymovestep*1.1)
    //scr_movenet(zombie_dy/2,0);
    
    }
yymovestep /= 2;
}

//6eme objet

if (place_meeting(x+xxmovestep, y, obj_barrilnuke)) {

    var block5 = instance_place(x+xxmovestep, y, obj_barrilnuke);
    with(block5){
    x+=(other.xxmovestep*1.1)
    //scr_movenet(zombie_dx/2,0);
    
    }
xxmovestep /= 2;
}

//VERTICAL
if (place_meeting(x,y+yymovestep, obj_barrilnuke)) {

    var block5 = instance_place(x,y+yymovestep,  obj_barrilnuke);
    with(block5){
    y+=(other.yymovestep*1.1)
    //scr_movenet(zombie_dy/2,0);
    
    }
yymovestep /= 2;
}
