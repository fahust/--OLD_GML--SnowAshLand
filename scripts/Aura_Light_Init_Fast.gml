/// Aura_Light_Init_Fast(xscale, yscale, angle, colour, alpha);

// Initialise the instance vars for the fast light

image_xscale = argument0;
image_yscale = argument1;
image_angle = argument2;
image_blend = argument3;
image_alpha = argument4;
